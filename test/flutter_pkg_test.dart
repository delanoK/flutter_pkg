import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_pkg/flutter_pkg.dart';

void main() {
  test('adds one to input values', () {
    /*final customButton = CustomButton();
    expect(customButton.addOne(2), 3);
    expect(customButton.addOne(-7), -6);
    expect(customButton.addOne(0), 1);*/
  });
}
